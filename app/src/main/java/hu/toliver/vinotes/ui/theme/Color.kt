package hu.toliver.vinotes.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primer palette (bordo/merlot) ───────────────────────────────────────────────
val Burgundy900 = Color(0xFF4A0010)   // dark mode surface
val Burgundy700 = Color(0xFF7B1D3A)   // primary (dark)
val Burgundy500 = Color(0xFFA83256)   // primary (light)
val Burgundy200 = Color(0xFFE8A0B4)   // primary container (light)
val Burgundy100 = Color(0xFFF7DCE4)   // background (light)

// ── Seconder palette (gold/amber) ──────────────────────────────────────────
val Gold600     = Color(0xFFB8860B)   // secondary (dark)
val Gold400     = Color(0xFFD4A843)   // secondary (light)
val Gold100     = Color(0xFFFFF3CD)   // secondary container (light)

// ── Neutrals ────────────────────────────────────────────────────────────────
val Cream50     = Color(0xFFFAF6F0)   // background (light)
val Cream100    = Color(0xFFF2EBE0)   // surface (light)
val WarmGray400 = Color(0xFF8C7B6E)   // onSurface variant
val WarmGray800 = Color(0xFF2E2420)   // onBackground (light)

// ── Dark mode surfaces ───────────────────────────────────────────────────────
val DarkSurface  = Color(0xFF1C1118)  // surface (dark)
val DarkBg       = Color(0xFF120D0F)  // background (dark)

// ── Semantics ───────────────────────────────────────────────────────────────
val RatingGold   = Color(0xFFFFB300)
val ErrorRed     = Color(0xFFCF4040)